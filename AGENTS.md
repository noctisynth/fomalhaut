# Fomalhaut Agent 协作约定

本文件适用于 Fomalhaut 仓库中的全部目录和任务。所有后续 Agent 在分析、设计、实现、
测试和交付工作时都必须遵守以下约定。

## 1. 修改授权

- 在用户明确要求实施或修改之前，不得直接修改任何代码或配置文件。
- 讨论方案、分析问题、审查代码或提出建议，不等同于获得实施授权。
- 未获得实施授权时，可以进行必要的只读检查，但必须保持工作区内容不变。
- 不得因为某项修改看起来简单、合理或属于后续步骤，就自行推断用户已经授权。
- 用户明确要求修改文档时，只能修改该请求范围内的文档；不得顺带实施代码或配置变更。

## 2. 设计文档优先

`docs/DESIGN.md` 是项目当前技术方案的权威来源，`TODO.md` 是设计与实际代码之间的差异
桥梁。

- 在已经获得实施授权的任务中，如果实施前或实施过程中发现任何潜在的技术方案变动，
  必须先更新 `docs/DESIGN.md`，再更新 `TODO.md`，最后才能继续实现相关代码。
- 更新顺序始终是：

  1. `docs/DESIGN.md`
  2. `TODO.md`
  3. 代码、配置和测试

- 不得先实现技术变动，再补写设计文档。
- 不得把新的架构决定、协议变更、安全边界、依赖选择、兼容性要求或实现约束只隐藏在
  代码中。
- `docs/DESIGN.md` 必须始终描述当前采用的最新技术方案，包括已经确认的决定和仍待验证
  的决定。
- `TODO.md` 必须根据最新设计维护：
  - 尚未实现的设计要求保留为未完成事项。
  - 已经由实际代码和必要验证满足的事项才能标记为完成。
  - 实际代码与设计出现差异时，必须将弥合差异所需的工作明确列出。
- 如果技术变动的影响或正确方向尚不明确，应立即停止相关实现并向用户确认，不能自行选择
  一个方向后继续。

## 3. Rust 错误处理与 panic 约束

- 生产 Rust 代码不得使用可能 panic 的 `unwrap()`。
- 对文件、网络、IPC、解析、配置、用户输入、系统调用和外部状态等预期内可能发生的失败，
  必须返回或转换为调用方可以处理的错误。
- 只有在类型系统、构造器，或同一函数内的穷尽分支已经证明某项内部不变量必然成立时，
  才允许在生产代码中使用 `expect()`。
- 每个获准使用的 `expect()` 都必须提供具体消息，说明由什么内部不变量保证该值存在或
  该操作必然成功。不得使用诸如 `"should work"`、`"cannot fail"` 等无法解释不变量的
  泛化消息。
- 不得使用 `expect()` 掩盖来自外部输入、运行环境、并发、I/O 或第三方服务的可恢复失败。
- 测试代码中为了表达断言或准备测试前置条件而使用的 `unwrap()`、`expect()` 和其他断言
  可以保留，但不得把本应由生产代码处理的失败转移到测试辅助代码中以规避上述约束。

## 4. Cargo manifest 与依赖管理

- 除非用户明确要求直接编辑，否则不得手动编辑 `Cargo.toml` 等 Cargo manifest 文件。
- 新增或移除依赖、crate、workspace member 或 crate 元数据时，必须使用能够表达该操作的
  Cargo CLI，例如：
  - `cargo add`
  - `cargo remove`
  - `cargo new`
  - `cargo init`
- 即使手动修改 manifest 更快，也不得用文本补丁、脚本、重定向或其他文件编辑方式绕过
  Cargo CLI。
- 如果 Cargo CLI 无法表达所需变更、命令执行失败，或者命令的预期结果不明确，必须立即
  停止并向用户确认。不得退回到手动编辑 `Cargo.toml`。
- 执行 Cargo CLI 后必须检查实际产生的 manifest 和工作区变更，确认它们与任务授权及
  `docs/DESIGN.md` 一致。
- 包版本是上述规则的特例：正常版本变更只能由 GitHub Actions 中的 Semifold CI 根据
  changeset 修改，不得使用 Cargo CLI、手工编辑或在本地执行版本命令。初始化独立版本时
  已经由用户单独授权的手工迁移不构成后续修改版本号的先例。
- GitHub Actions 中的 Semifold CI 同时更新的包间依赖版本属于发布事务的一部分，可以由
  Semifold 写入 manifest；其他第三方依赖仍必须使用 Cargo CLI 管理。

### 4.1 `cargo add` 的网络权限

- 当前受限环境中的普通 `cargo add` 无法访问 crates.io。
- 新增依赖时，应直接请求在受审批的网络环境中执行 `cargo add`，不要先运行一个预期会因
  网络限制失败的普通命令。
- 审批请求中的 `prefix_rule` 必须限定到目标 package，形式为：

  ```text
  cargo add -p <package>
  ```

- 不得申请不带目标 package 的宽泛 `cargo add` prefix rule。
- 不得因为网络审批未通过、网络不可用或 `cargo add` 失败而手动编辑 `Cargo.toml`。
  此时必须停止并向用户说明阻塞。

### 4.2 Semifold 版本与发布

- 仓库使用 Semifold，CLI 命令为 `smif`。
- 影响一个或多个可发布 crate 的变更应使用 `smif commit` 创建 changeset；纯文档、CI、
  仓库管理和不影响发布包的变更可以不创建 changeset。
- 不得手工编写 changeset 来绕过 Semifold CLI。
- 不得手工修改 package version。
- 本地和 Agent 环境严禁执行 `smif version` 与 `smif publish`，包括带 `--dry-run` 的
  调用。版本更新和发布由 GitHub Actions 中的 `semifold ci` 独占执行。
- 本地只允许使用 changeset、状态查询和配置维护命令，例如 `smif commit`、
  `smif status`、`smif config sync` 和 `smif config channel`。
- Semifold 配置和 package 列表应通过 `smif init`、`smif config sync` 等 CLI 维护。
- 如果 Semifold 命令失败、发现的 package 与 workspace 不一致或版本结果不明确，应停止
  并向用户确认，不得手工模拟 Semifold 的输出。

## 5. 任务完成与交付说明

每次完成任务时，最终回复除总结业务和技术产出外，还必须单独、明确说明本次采用的技术方案
相对于任务开始时既有设计发生了什么变化。

- 如果技术方案发生变化，必须说明：
  - 变化内容；
  - 变化原因；
  - 对架构、协议、安全、兼容性、依赖或后续工作的影响；
  - 已如何同步到 `docs/DESIGN.md` 和 `TODO.md`。
- 如果技术方案没有变化，也必须明确写明“本次技术方案相对既有设计无变动”。
- 不得将方案变动隐式包含在代码、配置、依赖或实现细节中而不告知用户。
- 交付说明还应列出实际完成的验证，以及任何尚未完成、被阻塞或需要用户决定的事项。
